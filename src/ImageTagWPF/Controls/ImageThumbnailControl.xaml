<UserControl x:Class="ImageTagWPF.Controls.ImageThumbnailControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ImageTagWPF.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="200"
             MouseDoubleClick="ImageThumbnailControl_OnMouseDoubleClick">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="5*" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>

        <Image x:Name="ThumbnailImage" Source="{Binding ImageSource}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="10,10,10,10"></Image>
        <TextBlock x:Name="FilenameText" Grid.Row="1" Text="{Binding Filename}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="10,10,10,10">
            
        </TextBlock>
    </Grid>
</UserControl>
